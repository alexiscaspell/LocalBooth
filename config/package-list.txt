# LocalBooth — base packages installed via apt before MakeInstall runs.
# MakeInstall (github.com/alexiscaspell/MakeInstall) handles the rest:
# Docker, Terraform, Terragrunt, kubectl, Python, AWS CLI, Cursor, SSH.

# --- prerequisites for MakeInstall ---
git
make
curl
wget
ca-certificates
gnupg
software-properties-common
lsb-release
unzip
jq

# --- system utilities ---
htop
vim
